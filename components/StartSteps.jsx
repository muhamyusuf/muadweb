const StartSteps = ({ text }) => (
  <div className="flex-row p-3 rounded-md bg-gradient-to-r from-orange-500 to-teal-500 flexCenter">
    <p className="flex-1 text-[25px] font-semibold text-white leading-[32.4px]">
      {text}
    </p>
  </div>
);

export default StartSteps;
